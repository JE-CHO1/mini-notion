import React from 'react';

/**
 * 가로선
 */
const HorizontalGap = (): JSX.Element => (
  <div
    style={{ backgroundColor: 'rgba(0, 0, 0, 0.1)', height: '1px', margin: '32px 0' }}
  />
);

export default HorizontalGap;
